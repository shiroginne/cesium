<h1>Layouts</h1>
<ul class="pages_tree">
<% @layouts.each do |layout| %>
  <li>
    <div class="info">
      <%= link_to layout.name, edit_admin_layout_url(layout) %>
      <div class="actions">
        <%= link_to 'destroy', admin_layout_url(layout), :method => :delete, :confirm => 'Are u shure?', :class => 'remove_entry' %>
      </div>
    </div>
  </li>
<% end %>
</ul>

<% content_for :additional_navigation do %>
  <%= link_to 'New layout', new_admin_layout_url %>
<% end %>
