page["page_#{@page.id}"].remove
page << <<-SCRIPT
  clearEmptyContainers();
  if ($('pages_tree').empty()) {
    $('additional_navigation').innerHTML += '#{link_to('New page', new_admin_page_path)}';
  }
SCRIPT
