<%= f.error_messages %>

<%= p controller.form_fields %>

<% controller.form_fields.each do |field| -%>
  <%= f.terbium_field field %>
<% end -%>

<div class="buttons">
  <%= f.submit 'Save' %>
  <%= link_to 'cancel', [:admin, controller.model.new] %>
</div>
